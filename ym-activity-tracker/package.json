{
  "name": "ym-activity-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute ym-activity-tracker --file=./schema.sql",
    "db:seed": "wrangler d1 execute ym-activity-tracker --file=./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
